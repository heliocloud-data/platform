env:
  account: 12345678
  region: us-east-1

vpc:
  type: new

# Destroying so we can clean up dev
userSharedBucket:
  destroyOnRemoval: True
  mountEnabled: True
  mountPath: "/mnt/s3shared"
  bucketPrefix: "myorganization-s3"
  
enabled:
  daskhub: True
  portal: True

# Configure per the domain of your respective organization
auth:
  domain_prefix: "myorganization-helio"
