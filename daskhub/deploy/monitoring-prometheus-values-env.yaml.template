# The contents of this file are auto-generated execution of the
# 03-monitoring.sh script during deployment.  It's intended to
# contain the environment specific settings for standing up
# a prometheus server to support metrics collection for the
# daskhub deployment.
server:
  service:
    annotations:
      service.beta.kubernetes.io/aws-load-balancer-ssl-cert: ${AWS_LOAD_BALANCER_SSL_CERT}
      service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
