
---
apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: portal

spec:
  allocateLoadBalancerNodePorts: true

  ports:
    - protocol: TCP
      port: 443
      targetPort: 80  # Port which science-console is running inside the container
  selector:
    app: portal
  type: LoadBalancer

